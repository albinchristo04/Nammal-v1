[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "npm install -g pnpm",
  "pnpm install --frozen-lockfile"
]

[phases.build]
cmds = ["pnpm --filter @nammal/db build", "pnpm --filter @nammal/api build"]

[start]
cmd = "cd apps/api && node dist/index.js"
